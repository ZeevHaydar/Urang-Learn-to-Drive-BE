import { app, port } from "../util/util";
import { json } from 'express';
import dotenv from 'dotenv';
import { Request, Response } from "express";

app.get('/dashboard', (req:Request, res:Response) => {
    
});